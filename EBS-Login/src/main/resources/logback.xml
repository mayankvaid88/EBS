<configuration>
    <appender name="MIDDLEWARE_ROLLING_APPENDER" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>/etc/logs/ebs//ebs-login-middleware.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>
                ebs-login-middleware.%d{yyyy-MM-dd}.log
            </fileNamePattern>
            <maxHistory>30</maxHistory>
            <totalSizeCap>3GB</totalSizeCap>
        </rollingPolicy>
        <encoder>
            <pattern>%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>
    <appender name="APP_ROLLING_APPENDER" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>/etc/logs/ebs/ebs-login-app.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>
                ebs-login-app.%d{yyyy-MM-dd}.log
            </fileNamePattern>
            <maxHistory>30</maxHistory>
            <totalSizeCap>3GB</totalSizeCap>
        </rollingPolicy>
        <encoder>
            <pattern>%X{traceId} %d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>
    <root level="info">
        <appender-ref ref="MIDDLEWARE_ROLLING_APPENDER"/>
    </root>
    <logger name="ebsLogin" level="INFO">
        <appender-ref ref="APP_ROLLING_APPENDER"/>
    </logger>
</configuration>